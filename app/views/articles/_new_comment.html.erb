<% if signed_in? %>
  <h4>Add a comment:</h4>
  <%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
      <%= f.label :body, "Comments" %><br>
      <%= f.text_area :body %>
    </p>
    
    <p>
      <%= f.submit "Submit" %>
    </p>
  <% end %>
<% end %>